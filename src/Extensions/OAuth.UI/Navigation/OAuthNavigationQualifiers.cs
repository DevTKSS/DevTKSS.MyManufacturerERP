namespace DevTKSS.Extensions.OAuth.UI.Navigation;

public static class OAuthNavigationQualifiers
{
    public const string SystemBrowser = "SystemBrowser";
    public const string Window = "Window";

    // public const string Dialog = "!"; // NOTE: Dialog qualifier is already existing in Uno.Extensions.Navigation as "!" char, therefore this is only placeholder if we need a individual one.
}
