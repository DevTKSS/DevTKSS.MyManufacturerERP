<Page
    x:Class="DevTKSS.MyManufacturerERP.Presentation.WebViewBrowserPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DevTKSS.MyManufacturerERP.Presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="50" />
      <RowDefinition Height="*" />
      <RowDefinition Height="50" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>
    <TextBox x:Name="AddressBar"
             Grid.Column="0"
             Grid.Row="0"
             KeyDown="AddressBar_KeyDown" />
    <Button x:Name="NavigateButton"
            Grid.Column="1"
            Grid.Row="0"
            Click="NavigateButton_Click"
            Content="Go" />

    <WebView2 x:Name="MyWebView"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Stretch"
              Source="{Binding CurrentUri,Mode=OneWay}"
              NavigationCompleted="MyWebView_NavigationCompleted"
              NavigationStarting="EnsureHttps"
              Grid.Column="0"
              Grid.Row="1" />

    <TextBlock x:Name="StatusBar"
               Text="WebView2"
               VerticalAlignment="Center"
               Grid.Row="2"
               Grid.Column="0"
               Grid.ColumnSpan="2"
               Margin="10,0,10,0" />
  </Grid>
</Page>
